from setuptools import setup


setup(
        name='s3Helper',
        version='0.1.0',
        author='Nicolas Tamez',
        packages=['s3Helper'],
        install_requires=[
            'boto3'
            ]
        )
